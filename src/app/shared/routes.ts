export type Routes = {
  home: string;
  userLogin: string;
  userLogout: string;
  gameNew: string;
};

export const ROUTES: Routes = {
  home: '/',
  userLogin: 'user/login',
  userLogout: 'user/logout',
  gameNew: 'game/new',
};
